# Input the base and power from the user
base = float(input("Enter the base: "))
power = float(input("Enter the power: "))

# Calculate base raised to the power
result = base ** power

# Output the result
print(f"{base} raised to the power of {power} is: {result:.2f}")
